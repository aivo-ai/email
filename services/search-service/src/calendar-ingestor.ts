import { CalendarEventIngest } from './ingestors';

export async function ingestCalendarEvent(event: CalendarEventIngest) {
  // TODO: Index calendar event in OpenSearch
  // Example: opensearch.index({ index: 'calendar', body: event })
}
