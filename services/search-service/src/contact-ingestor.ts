import { ContactIngest } from './ingestors';

export async function ingestContact(contact: ContactIngest) {
  // TODO: Index contact in OpenSearch
  // Example: opensearch.index({ index: 'contacts', body: contact })
}
